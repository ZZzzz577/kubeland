---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: buildsettings.app.kubeland.com
spec:
  group: app.kubeland.com
  names:
    kind: BuildSettings
    listKind: BuildSettingsList
    plural: buildsettings
    singular: buildsettings
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BuildSettings is the Schema for the buildsettings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BuildSettings
            properties:
              dockerfile:
                type: string
              gitUrl:
                properties:
                  repo_name:
                    type: string
                  repo_path:
                    type: string
                required:
                - repo_name
                - repo_path
                type: object
              imageUrl:
                properties:
                  repo_name:
                    type: string
                required:
                - repo_name
                type: object
            required:
            - dockerfile
            - gitUrl
            - imageUrl
            type: object
          status:
            description: status defines the observed state of BuildSettings
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
